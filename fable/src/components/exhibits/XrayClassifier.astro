<figure class="exhibit" data-exhibit="xrayclassifier" data-src="/data/xray-samples.json" id="fig-06">
  <div class="exhibit__bar">
    <span class="mono"><strong>Fig. 06</strong> · chest X-ray classifier</span>
    <span class="ticks" aria-hidden="true"></span>
    <span class="mono" data-mode-label>mode: multi-label CNN</span>
  </div>

  <!-- Sample selector buttons -->
  <div class="ctl-row" role="group" aria-label="X-ray sample selection" data-samples>
    <noscript><p class="mono">Interactive exhibit requires JavaScript.</p></noscript>
  </div>

  <!-- Main display area -->
  <div class="xray-display">
    <!-- X-ray image viewer -->
    <div class="xray-viewer">
      <div class="xray-container">
        <svg class="xray-placeholder" data-xray-image viewBox="0 0 200 240" role="img" aria-label="Chest X-ray image">
          <rect width="200" height="240" fill="#1a1c20" />
          <text x="100" y="120" text-anchor="middle" fill="#8a8c88" font-family="monospace" font-size="12">
            Loading X-ray...
          </text>
        </svg>
        <!-- Heatmap overlay (for activation visualization) -->
        <canvas class="heatmap-overlay" data-heatmap width="200" height="240" style="display: none;"></canvas>
      </div>

      <div class="xray-controls">
        <button class="btn btn--small" type="button" data-toggle-heatmap>
          Show Activation Heatmap
        </button>
        <div class="confidence-display">
          <span class="mono">Overall Confidence:</span>
          <span class="confidence-value" data-confidence>--</span>
        </div>
      </div>
    </div>

    <!-- Disease predictions panel -->
    <div class="predictions-panel">
      <div class="panel-header">
        <span class="mono">Disease Probabilities</span>
        <div class="threshold-control">
          <label class="mono" for="threshold-slider">Threshold: <span data-threshold-value>0.5</span></label>
          <input type="range" id="threshold-slider" data-threshold min="0.1" max="0.9" value="0.5" step="0.05" />
        </div>
      </div>

      <div class="disease-list" data-predictions>
        <!-- Disease probability bars will be populated here -->
      </div>
    </div>
  </div>

  <!-- Model performance stats -->
  <div class="model-stats">
    <div class="stat-group">
      <span class="mono stat-label">Architecture:</span>
      <span class="mono stat-value">CNN (32→64→128 filters)</span>
    </div>
    <div class="stat-group">
      <span class="mono stat-label">Binary Accuracy:</span>
      <span class="mono stat-value">94.8%</span>
    </div>
    <div class="stat-group">
      <span class="mono stat-label">AUC:</span>
      <span class="mono stat-value">0.824</span>
    </div>
    <div class="stat-group">
      <span class="mono stat-label">Current Prediction:</span>
      <span class="mono stat-value" data-current-prediction>Select a sample</span>
    </div>
  </div>

  <!-- Caption -->
  <figcaption class="figcap mono">
    Multi-label CNN trained on ChestMNIST (14 disease classes). Each X-ray can have multiple conditions.
    Model focuses attention on clinically relevant regions.
  </figcaption>
</figure>

<style scoped>
.xray-display {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  margin: 1.5rem 0;
}

.xray-viewer {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.xray-container {
  position: relative;
  width: 100%;
  max-width: 300px;
  background: var(--graphite);
  border-radius: 4px;
  overflow: hidden;
}

.xray-placeholder {
  width: 100%;
  height: auto;
  display: block;
}

.heatmap-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  mix-blend-mode: screen;
}

.xray-controls {
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
}

.confidence-display {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.5rem;
  background: var(--anechoic);
  border-top: 2px solid var(--baffle);
}

.confidence-value {
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--graphite);
}

.predictions-panel {
  background: var(--anechoic);
  border-top: 2px solid var(--baffle);
  border-radius: 4px;
  overflow: hidden;
}

.panel-header {
  padding: 1rem;
  border-bottom: 1px solid var(--baffle);
  background: var(--anechoic);
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 1rem;
}

.threshold-control {
  display: flex;
  flex-direction: column;
  gap: 0.3rem;
  min-width: 150px;
}

.threshold-control input[type="range"] {
  accent-color: var(--rec-red);
}

.disease-list {
  max-height: 300px;
  overflow-y: auto;
  padding: 0;
}

.disease-item {
  display: flex;
  align-items: center;
  padding: 0.6rem 1rem;
  border-bottom: 1px solid var(--baffle);
  transition: background-color 0.2s ease;
}

.disease-item:last-child {
  border-bottom: none;
}

.disease-item.positive {
  background: rgba(229, 35, 27, 0.1);
}

.disease-info {
  flex: 1;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.disease-name {
  font-size: 0.875rem;
  color: var(--graphite);
}

.disease-abbrev {
  font-size: 0.75rem;
  color: var(--foam);
  margin-left: 0.5rem;
}

.probability-bar {
  width: 80px;
  height: 8px;
  background: var(--baffle);
  border-radius: 4px;
  overflow: hidden;
  margin: 0 1rem;
}

.probability-fill {
  height: 100%;
  background: var(--foam);
  transition: width 0.3s ease, background-color 0.3s ease;
  border-radius: 4px;
}

.probability-fill.above-threshold {
  background: var(--rec-red);
}

.probability-value {
  font-size: 0.8rem;
  color: var(--graphite);
  font-weight: 600;
  min-width: 3rem;
  text-align: right;
}

.model-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
  margin: 1.5rem 0;
  padding: 1rem;
  background: var(--anechoic);
  border-top: 2px solid var(--baffle);
}

.stat-group {
  display: flex;
  flex-direction: column;
  gap: 0.3rem;
}

.stat-label {
  font-size: 0.8rem;
  color: var(--foam);
}

.stat-value {
  font-size: 0.875rem;
  color: var(--graphite);
}

@media (max-width: 768px) {
  .xray-display {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .panel-header {
    flex-direction: column;
    align-items: stretch;
  }

  .threshold-control {
    min-width: auto;
  }

  .model-stats {
    grid-template-columns: 1fr 1fr;
  }
}
</style>